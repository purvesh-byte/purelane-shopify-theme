{
  "name": "purelane-shopify-theme",
  "version": "1.0.0",
  "description": "Purelane plant-based homecare — Shopify theme built on Dawn",
  "private": true,
  "scripts": {
    "dev": "shopify theme dev ",
    "push": "shopify theme push ",
    "pull": "shopify theme pull",
    "check": "shopify theme check",
    "info": "shopify theme info"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
